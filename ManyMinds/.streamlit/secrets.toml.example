# HOW TO USE:
# 1. Copy this file and rename the copy to  secrets.toml  (same folder).
# 2. Paste your real Groq key between the quotes.
# 3. secrets.toml is git-ignored, so your key never gets pushed to GitHub.
#
# Get a free key at: https://console.groq.com/keys

GROQ_API_KEY = "gsk_paste_your_real_key_here"
